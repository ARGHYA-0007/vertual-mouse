# vertual-mouse
A virtual mouse system controlled using hand gestures. The cursor is moved using the index finger. Single and double clicks are performed by closing the index and middle fingers. Scrolling is controlled using pinky and ring finger gestures, enabling touch-free interaction.
